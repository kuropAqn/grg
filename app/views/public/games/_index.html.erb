<div class="m-3">
<table class="table table-border">
  <thead>
    <tr>
      <th></th>
      <th>タイトル</th>
      <th>価格</th>
      <th>ジャンル</th>
    </tr>
  </thead>
  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= link_to game_path(game.id) do %>
              <%= image_tag game.game_image, width: 200 %>
            <% end %>
        </td>
        <td>
          <h5><%= link_to game.title, game_path(game.id) %></h5>
        </td>
        <td><%= game.price %>円</td>
        <td><%= game.genre.name if game.genre %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>